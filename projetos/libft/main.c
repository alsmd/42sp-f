#include "libft.h"
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <limits.h>
#include <errno.h>

extern int errno;

int	main(void)
{
	printf("%s", ft_itoa(INT_MIN));
	/* while (array[index])
	{
		printf("%s\n", array[index]);
		index++;
	} */
	return (0);
}